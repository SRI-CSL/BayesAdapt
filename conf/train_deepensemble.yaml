defaults:
  - default #conf/default.yaml
  - lora: default
  - lora/wrapper: deepensemble
  - _self_

samples:
  train:
    backbone: ${lora.wrapper.ensemble_size}
    last_layer: 1
  test:
    backbone: ${lora.wrapper.ensemble_size}
    last_layer: 1

n_eval_trials: 1
